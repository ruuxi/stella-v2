import "./{{ID}}.css";

export function {{COMPONENT}}View() {
  return (
    <div className="{{ID}}-app">
      <header className="{{ID}}-hero">
        <h1 className="{{ID}}-title">{{LABEL}}</h1>
        <p className="{{ID}}-lead">Replace this with the real surface.</p>
      </header>
      <section className="{{ID}}-body">
        {/* Build the surface here. Let layout match the feature; use the full page. */}
      </section>
    </div>
  );
}
