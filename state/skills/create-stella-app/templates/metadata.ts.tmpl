import { {{ICON}} } from "@/shell/sidebar/SidebarIcons";
import type { AppMetadata } from "../_shared/app-metadata";

const metadata: AppMetadata = {
  id: "{{ID}}",
  label: "{{LABEL}}",
  icon: {{ICON}},
  route: "/{{ID}}",
  slot: "{{SLOT}}",
  order: {{ORDER}},
};

export default metadata;
