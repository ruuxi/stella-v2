import "./{{ID}}.css";

export function {{COMPONENT}}View() {
  return (
    <div className="{{ID}}-app" data-stella-label="{{LABEL}}">
      <h1 className="{{ID}}-title">{{LABEL}}</h1>
      <p className="{{ID}}-subtitle">Replace this with the real surface.</p>
    </div>
  );
}
