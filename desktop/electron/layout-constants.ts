/**
 * Shared layout constants for the main process.
 * Renderer mirrors: src/constants/layout.ts (must stay in sync).
 */
export const RADIAL_SIZE = 280
export const FULL_SHELL_MIN_SIZE = { width: 480, height: 600 } as const
export const MINI_SHELL_SIZE = { width: 480, height: 700 } as const
export const MINI_SHELL_MIN_SIZE = { width: 420, height: 560 } as const
export const MINI_SHELL_MAX_SIZE = { width: 500, height: 900 } as const
