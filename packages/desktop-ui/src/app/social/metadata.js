import { CustomUsers } from "@/ui/nav-icons";
const metadata = {
    id: "social",
    label: "Social",
    icon: CustomUsers,
    route: "/social",
    slot: "top",
    order: 40,
};
export default metadata;
