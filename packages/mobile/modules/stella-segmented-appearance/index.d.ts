export function applySegmentedControlAppearance(options: {
  /** Track colour, "#RRGGBB" or "#RRGGBBAA"; null restores the default. */
  background: string | null;
  /** Selected-segment colour, "#RRGGBB" or "#RRGGBBAA"; null restores the default. */
  selected: string | null;
}): void;
