import { CustomSettings } from "@/ui/nav-icons";
const metadata = {
    id: "settings",
    label: "Settings",
    icon: CustomSettings,
    route: "/settings",
    slot: "bottom",
    order: 40,
};
export default metadata;
