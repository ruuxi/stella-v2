export function GrCanvasView() {
  return (
    <div className="gr-canvas-app">
      <header className="gr-canvas-hero">
        <h1 className="gr-canvas-title">GR Canvas</h1>
        <p className="gr-canvas-lead">Replace this with the real surface.</p>
      </header>
      <section className="gr-canvas-body">
        {/* Build the surface here. Let layout match the feature; use the full page. */}
      </section>
    </div>
  );
}
